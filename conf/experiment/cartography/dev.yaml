defaults:
  - required_pub_conditions: dev
  - _self_ # stuff below can override stuff above  

target_size: 100
max_failed_expansions: 2
n_pubs_per_exp_max: 10

# for embed_documents; may be useful to reduce under memory constraints
batch_size: 64

# API call size
call_size: null
